resources:
  - name: deploy_aws_img
    type: image
    integration: ship-docker
    pointer:
      sourceName: "shippabledocker/pipelinev2"
    seed:
      versionName: "deploy.2"

  - name: test_aws_cli
    type: cliConfig
    integration: "quay-0ct5"
