# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  #- oraclejdk8
env:
  - single=env
  - multiple=secondenv
 
#matrix:
 # allow_failures:
  #  - jdk: oraclejdk7 
build:
  ci:
    - java -version  
    - echo $single
    - echo $multiple
    #- if [ "$SHIPPABLE_JDK_VERSION" == "oraclejdk7" ]; then foobar; fi
    - echo $custom
#    - sleep 8m
   
