# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
  #- oraclejdk7
  #- oraclejdk8
  
env:
  global:
    - test: value1
matrix:
  include:
    - jdk: oraclejdk7 
      env: 
       # - custom: fubu
        - boo: kubu
build:
  ci:
    - java -version  
    - echo $custom
    - echo $boo
    - echo $test
    - echo $ENV
    - echo $check
   
   # - if [ "$SHIPPABLE_JDK_VERSION" == "oraclejdk7" ]; then foobar; fi
    #- echo $custom
#    - sleep 8m
   
integrations:
  generic:
    - integrationName: key-val  
    
