language: java

jdk:
   #- openjdk7
   #- oraclejdk7
   #- openjdk6
   - oraclejdk8

build:
  pre_ci:
    - docker build -t=shippabledocker/testgeneric:tag1 .
 #   - docker build -t=quay.io/shippablequay/hitchcock3_sample_java .

  pre_ci_boot:
    image_name: shippabledocker/sample-node
    image_tag: latest.40
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    
    - docker push shippabledocker/testgeneric:tag1 

integrations:
  hub:
    - integrationName: test-docker-new
      type: dockerRegistryLogin 
    #  branches:
     #   only:
      #    - master
      
      

     
     
     
     
     
     
     
     
     



