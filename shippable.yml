# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
 # - oraclejdk7
  #- oraclejdk8
env:
  global:
    - test=foo
matrix:
  include:
    - jdk: oraclejdk7 
build:
  ci:
    - java -version  
    - echo $custom
    - echo $test
    - if [ "$SHIPPABLE_JDK_VERSION" == "oraclejdk7" ]; then foobar; fi
    #- echo $custom
#    - sleep 8m
   
