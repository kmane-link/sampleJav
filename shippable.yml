language: java

jdk:
   #- openjdk7
   #- oraclejdk7
   #- openjdk6
   - oraclejdk8

build:
#  pre_ci:
 #   - docker build -t=quay.io/shippablequay/hitchcock3_sample_java .

  pre_ci_boot:
    image_name: quay.io/revathird/testnodeimg
    image_tag: latest
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=quay.io/revathird/testgeneric .
    - docker push quay.io/revathird/testgeneric

integrations:
  hub:
    - integrationName: test_quay_generic
      type: quayLogin
    #  branches:
     #   only:
      #    - master
      
      

     
     
     
     
     
     
     
     
     



